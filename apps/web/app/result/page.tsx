import type { JSX } from 'react';
import { Button } from '@repo/ui/button';

export default function Home(): JSX.Element {

    return (
        <div>
            why?
            <Button style={{ width: 335, margin: '20px 40px' }}>
                다음
            </Button>
        </div>
    );
}
